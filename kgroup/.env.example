# Server only: never expose these values in browser JavaScript.
DATABASE_URL=
SESSION_COOKIE_NAME=kg_session
SESSION_TTL_DAYS=14
APP_URL=http://localhost:8888

# Email confirmation and password reset, sent through Resend.
RESEND_API_KEY=
EMAIL_FROM=

# Optional Google OAuth. Leave empty to keep Google login disabled.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
